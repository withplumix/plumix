# Generated by `prepack` from each `examples/*`. Lives only inside the
# published tarball — the workspace scaffolder resolves the examples
# directly via a relative path. See `scripts/sync-template.mjs`.
/templates/
