# Generated by `prepack` from `examples/minimal/`. Lives only inside
# the published tarball — the workspace scaffolder resolves the
# example directly via a relative path. See `scripts/sync-template.mjs`.
/templates/
