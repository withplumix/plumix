# Local values for a built server. Nothing loads this file on its own: run
# `node --env-file=.env dist/server/worker.js`. A production process reads
# only the environment its platform injects.
#
# Where the server listens.
PORT=3000
HOST=0.0.0.0
