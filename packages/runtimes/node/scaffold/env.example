# Copy to `.env` for local values. A production process reads only the
# environment its platform injects; nothing here is loaded for it.
#
# Where the server listens when run with `node dist/server/worker.js`.
PORT=3000
HOST=0.0.0.0
