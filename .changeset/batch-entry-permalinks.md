---
"plumix": minor
---

Adds `buildEntryPermalinks` and `buildTermArchiveUrls`, which build URLs for a list of entries or terms and load every nested row's ancestor chain in one query instead of one query per row.
