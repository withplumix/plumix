---
"@plumix/plugin-blog": patch
"@plumix/plugin-comments": patch
"@plumix/plugin-og": patch
---

Updates the `relatedPosts` and `comments` template deps, and the dep declarations an og card accepts, to the keyed template-dep registration in `plumix`. What these deps render does not change.
