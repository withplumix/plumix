---
"plumix": minor
---

`ImageDelivery` gains an optional `acceptsRelativeSources` flag for a slot that resolves a same-origin relative source itself, such as an in-process transformer, rather than fetching it over the network as a CDN does.
