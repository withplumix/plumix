---
"create-plumix-app": minor
---

Scaffolds new projects on TypeScript 7 (`typescript@^7.0.2`), the native compiler with a much faster `tsc`. Existing projects are unaffected; they can upgrade on their own now that `plumix/vite` no longer needs the TypeScript compiler API.
