---
"@plumix/plugin-og": patch
---

Carries `storedMeta` on the dev-preview sample entry and term, following the field core's
`ResolvedEntry` / `ResolvedTerm` gained so `.whereMeta()` compares against the stored meta bag.
