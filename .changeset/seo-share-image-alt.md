---
"@plumix/plugin-seo": minor
---

Adds `og:image:alt` and `twitter:image:alt` when the resolved share image names what it shows — a `featured` or `ogImage` role carries its media row's alt text, and a `seo:og_image` subscriber may return its own.
