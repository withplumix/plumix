---
"@plumix/core": minor
---

Adds `createTestContext` and `applyTestSchema` to `plumix/test` — a real `AppContext` for tests that call a service function directly, and a one-liner for creating a drizzle schema module's tables on an existing test db.
