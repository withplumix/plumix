---
title: Overview
description: What a Plumix deploy is made of, and the sequence that takes a recipe site from your machine to a live Worker.
tier: P0
sidebar:
  order: 10
---

import { Aside, Steps, TabItem, Tabs } from "@astrojs/starlight/components";

A Plumix site deploys as one Cloudflare Worker. The build turns `plumix.config.ts` into a worker entry, stages the admin and your static files beside it, and every resource the site reads at runtime arrives as a binding on the Worker env.

## What a deploy is

Two files decide what a deploy is. `plumix.config.ts` picks the runtime adapter and names the binding each slot reads. `wrangler.jsonc` declares those bindings as real Cloudflare resources and sets the Worker's compatibility flags. A binding name appears once in each file, and the two spellings have to match or the first request fails with a list of what is missing.

Secrets are in neither file. They arrive on `env` when a request runs, which is why a secret-bearing slot takes an `(env) => value` resolver instead of a literal. Read [Secrets](/deployment/secrets/) for that shape.

Everything else is a build output. `plumix build` writes a generated worker entry, a generated Drizzle schema, and a static directory holding the admin SPA plus anything in your `public/` folder. You commit none of it.

There is no origin server. The Worker answers the public site, the admin at `/_plumix/admin`, and the cron invocations, all from the same script.

## Running the commands

The scaffolder adds `plumix` to your project rather than to your PATH, so reach it through your package runner.

<Tabs syncKey="pkg">
  <TabItem label="pnpm">

```sh
pnpm exec plumix deploy
```

  </TabItem>
  <TabItem label="npm">

```sh
npx plumix deploy
```

  </TabItem>
  <TabItem label="yarn">

```sh
yarn plumix deploy
```

  </TabItem>
</Tabs>

The rest of this page writes the commands bare. `wrangler` works the same way.

## Quickstart

This is the path from a scaffolded recipe site to a live one. It assumes a site made with `pnpm create plumix-app` and a signed-in `wrangler`.

<Steps>

1. **Build the site.**

   ```sh
   plumix build
   ```

   The build fails here rather than in production if a plugin registration or a config slot is wrong, because it constructs the whole app before Vite starts.

2. **Create the database.** The scaffolded `wrangler.jsonc` ships a placeholder `database_id` that works only for local dev.

   ```sh
   wrangler d1 create recipes
   ```

   Paste the returned id into the `d1_databases` entry in `wrangler.jsonc`.

3. **Generate the migrations.**

   ```sh
   plumix migrate generate
   ```

   This writes `.plumix/schema.ts` from your config, then runs `drizzle-kit generate` over it. The SQL lands in `drizzle/`, which you do commit.

4. **Apply them to the remote database.**

   ```sh
   plumix migrate apply --remote
   ```

   Without `--remote` the same command applies to your local D1 copy. Everything after the command name passes through to `wrangler d1 migrations apply`.

5. **Deploy.**

   ```sh
   plumix deploy
   ```

   This is `wrangler deploy` with your arguments appended.

6. **Enrol the first passkey.** Open `/_plumix/admin` on the deployed origin. The first admin has to arrive through the passkey bootstrap rail, so do this before inviting anyone.

</Steps>

## What the build produces

`plumix build` regenerates the `.plumix/` directory before Vite starts. Nothing in it is yours to edit, and every file carries a header saying so.

`.plumix/worker.ts` is the Worker entry, and `wrangler.jsonc` points its `main` at it. The file imports your config, calls `buildApp` once per isolate, and hands each request to `config.runtime.buildFetchHandler(app)`. Regenerating it on every build is what keeps the entry in step with the config.

`.plumix/schema.ts` is the Drizzle schema assembled from core's tables plus every table a plugin contributes. `plumix migrate generate` reads it.

`.plumix/client-entry.ts` is the browser bundle's entry, and it carries the CSS files your theme declared. Plumix emits it even when it is empty, because the Vite plugin lists it as a client entry unconditionally.

`.plumix/public` holds the static half: your `public/` directory copied in, and the precompiled admin SPA staged under `_plumix/admin/`.

Vite then builds twice, client first and worker second, because the worker bakes the client's asset manifest into its own bundle. The output is a `dist/client` directory of static assets and a `dist/<worker-name>/` directory holding the worker bundle and the `wrangler.json` the Cloudflare Vite plugin writes.

## Build time against request time

Your config module evaluates once, on your machine, during the build. Anything it reads from `process.env` is baked in. Nothing in the Worker env exists yet.

Bindings and secrets exist only inside a request. The runtime adapter connects each slot per request by handing it the Worker `env`, so `d1({ binding: "DB" })` stores the string `"DB"` at build time and resolves it to a live D1 handle when a request arrives.

That split is why the secret-bearing slots take a resolver. `mailer`, an OAuth provider's `clientSecret`, R2's S3 credentials and `passkey.origin` all accept either a literal or an `(env) => value` function, and the framework calls the function once per isolate with the real env.

## Migrations

Plumix generates migrations from your config rather than from a hand-written schema file. Registering an entry type adds no table, so most content modelling needs no migration at all. A plugin that ships its own tables does.

Run `plumix migrate generate` after adding or removing such a plugin, commit the SQL in `drizzle/`, and apply it with `plumix migrate apply --remote` before the deploy that needs it. The command reads the database name out of your wrangler config, so pass one explicitly only when the config declares more than one D1 database.

## Scheduled tasks

Core registers two scheduled tasks: a daily session cleanup and a `publish-scheduled` task that flips due entries from `scheduled` to `published`. Neither runs unless the Worker declares a matching cron trigger.

```jsonc
// wrangler.jsonc
{
  "triggers": {
    "crons": ["0 3 * * *", "*/5 * * * *"],
  },
}
```

<Aside type="caution" title="A missing trigger is silent">
  With no cron trigger, a scheduled recipe stays scheduled and nothing reports
  the problem. The scaffolded `wrangler.jsonc` ships both crons, so this bites
  when a hand-written config drops them.
</Aside>

## Preview deploys

Workers Builds gives every branch its own `*.workers.dev` host. A passkey is anchored to a hostname, so a credential enrolled on production will not verify on a preview unless the two share a registrable domain.

`cloudflareDeployOrigin` handles that. It reads the Workers Builds environment, anchors `rpId` to `<account>.workers.dev`, and returns an `allowedOrigins` wildcard covering every host beneath it, so one passkey spans production and each preview branch. See [Cloudflare Workers](/deployment/cloudflare/) for the full shape and for what changes when production runs on a custom domain.

## Related

Your passkey origin is a deploy concern and an identity concern at once, so [Passkeys](/access/passkeys/) is worth reading alongside this page. Scheduled publishing is described from the content side in [Statuses and Publishing](/content-modelling/statuses/). The generated files under `.plumix/` are catalogued from the repository side in [Project Structure](/getting-started/project-structure/).

Two pages in this section are not written yet, and what they cover already ships. CLI Reference is the exhaustive listing of the seven commands and the five global flags. Runtime Adapters describes the `RuntimeAdapter` contract, which is what a runtime other than Cloudflare Workers would implement.

## Next steps

Read [Cloudflare Workers](/deployment/cloudflare/) next for the adapters that fill each slot, then [Bindings and Environment](/deployment/bindings/) for how a name in `wrangler.jsonc` reaches your config. [Secrets](/deployment/secrets/) covers the values neither file may contain.

If you have not deployed anything yet, [Deploy Your Site](/getting-started/deploy/) is the shorter path and comes back here for depth. [Configuration](/getting-started/configuration/) is the reference for every config slot named above.
