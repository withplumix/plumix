---
import Default from "@astrojs/starlight/components/MarkdownContent.astro";

import Screenshot from "./Screenshot.astro";

const { screenshot } = Astro.locals.starlightRoute.entry.data;
---

<Default>
  {screenshot && <Screenshot {...screenshot} />}
  <slot />
</Default>
