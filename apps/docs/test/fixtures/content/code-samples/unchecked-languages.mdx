---
title: Unchecked Languages
description: A page whose only fenced blocks are shell and configuration.
---

A lede that stands on its own, because an agent reads it first.

## Overview

Neither block below is TypeScript, and neither would survive being checked as if
it were.

```bash
pnpm add plumix && plumix dev --port 5173
```

## Quickstart

```json
{ "compilerOptions": { "strict": true } }
```

## Related

- The page whose samples compile.

## Next steps

- Read the opt-out marker.
