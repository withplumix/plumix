An underscore-prefixed file, which the collection glob excludes just as it does
an underscore-prefixed directory.

```ts
import { text } from "plumix/fields";

export const subtitle = text("subtitle").label("Subtitle").maxLength(120);
```
