{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "topo": {
      "dependsOn": ["^topo"]
    },
    "i18n:compile": {
      "inputs": ["locales/*.po"],
      "outputs": ["locales/*.mjs", "locales/*.d.mts"]
    },
    "build": {
      "dependsOn": ["^build", "i18n:compile"],
      "outputs": [".cache/build.tsbuildinfo", "dist/**"],
      "env": ["WORKERS_CI", "WORKERS_CI_BRANCH", "PLUMIX_DEV"]
    },
    "dev": {
      "dependsOn": ["i18n:compile"],
      "cache": false,
      "persistent": true
    },
    "format": {
      "outputs": [".cache/.prettiercache"],
      "outputLogs": "new-only"
    },
    "lint": {
      "dependsOn": ["^topo", "^build", "i18n:compile"],
      "outputs": [".cache/.eslintcache"]
    },
    "typecheck": {
      "dependsOn": ["^topo", "^build", "i18n:compile"],
      "outputs": [".cache/tsbuildinfo.json"]
    },
    "test:unit": {
      // Low-level tests: resolve workspace imports to source and stub
      // name-imported i18n catalogs (see tooling/vitest/source-resolver.ts), so
      // they depend on nothing generated — no build, no i18n:compile.
      "dependsOn": []
    },
    "test:build": {
      // Tests that spin up a real Vite build and inspect the emitted artifacts
      // (`*.build.test.ts`). Only plumix has these; they need the build graph.
      "dependsOn": ["build", "^build"]
    },
    "test:e2e": {
      "dependsOn": ["build", "^build"],
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "//#clean": {
      "cache": false
    },
    "publint": {
      "dependsOn": ["build"]
    },
    "attw": {
      "dependsOn": ["build"]
    },
    "i18n:check": {
      "dependsOn": ["plumix#build"],
      "cache": false
    },
    "i18n:ratchet:check": {
      "cache": false
    }
  },
  "globalPassThroughEnv": [
    "NODE_ENV",
    "CI",
    "npm_lifecycle_event",
    "NO_COLOR",
    "FORCE_COLOR",
    "PLUMIX_VERBOSE"
  ]
}
