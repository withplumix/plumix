# dependencies
node_modules
.pnp
.pnp.js

# testing
coverage
playwright-report
test-results

# production
build
dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env
.env*.local
!.env.example

# turbo
.turbo

# caches (tsbuildinfo, eslint, prettier)
.cache

# cloudflare
.wrangler/

# drizzle-kit generated migrations (committed under packages once a release flow is in place)
.drizzle/
.output/
# `plumix migrate generate` output in playgrounds + examples — generated
# fresh on every e2e run, never committed.
drizzle/

# worker-driven plugin e2e: storageState.json is written by globalSetup
# at the package root; playwright traces / reports land in test-results/
# + playwright-report/ (already covered above).
storageState.json

# plumix generated
.plumix/

# IDE
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Internal planning and reference docs (not committed)
PLAN.md
docs/reference/
.claude/
